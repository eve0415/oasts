openapi: 3.1.0
info:
  title: Response media parity
  version: 1.0.0
paths:
  /items:
    get:
      operationId: getItems
      responses:
        "200":
          description: Items
          content:
            text/xml:
              schema:
                type: object
                required: [id]
                properties:
                  id:
                    type: integer
