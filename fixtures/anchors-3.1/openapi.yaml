openapi: 3.1.0
info:
  title: JSON Schema anchor references
  version: 1.0.0
paths: {}
components:
  schemas:
    SameFileReference:
      $ref: '#SameFile'
    SameFileTarget:
      $anchor: SameFile
      type: object
      required:
        - name
      properties:
        name:
          type: string
    CrossFileReference:
      $ref: './second.yaml#CrossFile'
