---
title: Reference
description: The complete configuration surface and the command line.
sidebar:
  order: 5
  group:
    label: Reference
---

<CardGrid>
  <Card title="Configuration" icon="ph:sliders">
    Every block of `oasts.yaml` — input, artifacts, output layout, type shape, client, validation, filters, naming, docs, limits.
    [Read more](/reference/configuration).
  </Card>
  <Card title="CLI" icon="ph:terminal-window">
    Commands, flags, config discovery, and exit codes.
    [Read more](/reference/cli).
  </Card>
</CardGrid>

The configuration file is also validated against a published JSON Schema, and a typed `oasts.config.ts` gets the same surface as editor completion.
