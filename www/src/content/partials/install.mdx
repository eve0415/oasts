<PackageManagers pkg="@oasts/cli" type="add" dev />

The package is `@oasts/cli` and the command it installs is `oasts`.
It ships a native binary, so there is nothing to compile locally.
Node 24 or newer.
